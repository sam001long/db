name: Vendor three.js into docs/animation/vendor

on:
  workflow_dispatch: {}   # 手動觸發

permissions:
  contents: write

jobs:
  vendor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node (for npm)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install three@0.161.0 and copy vendor files
        run: |
          set -e
          mkdir -p tmp-npm && cd tmp-npm
          npm init -y >/dev/null 2>&1
          npm i three@0.161.0
          cd ..
          mkdir -p docs/animation/vendor
          cp tmp-npm/node_modules/three/build/three.min.js docs/animation/vendor/three.min.js
          cp tmp-npm/node_modules/three/examples/js/loaders/GLTFLoader.js docs/animation/vendor/GLTFLoader.js
          echo "Files now in docs/animation/vendor:"
          ls -al docs/animation/vendor

      - name: Commit vendor files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/animation/vendor/three.min.js docs/animation/vendor/GLTFLoader.js
          git commit -m "vendor: add three.min.js + GLTFLoader.js (0.161.0)" || echo "No changes to commit"
          git push
